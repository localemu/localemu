"""Docker-backed RDS instances for LocalEmu.

When RDS_DOCKER_BACKEND=1 is set, CreateDBInstance starts real MySQL/PostgreSQL/MariaDB
Docker containers with actual database engines that users can connect to.
"""
